import Backbone from 'backbone';

export default class DeepModel extends Backbone.Model {

  // TODO: implement

  static extend(propProps, staticProps) {
    return Backbone.Model.extend(propProps, staticProps);
  }

}
