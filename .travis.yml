language: node_js

node_js:
  - "7"
  - "6"
  - "4"

cache:
  directories:
    - node_modules

install:
  - npm install -g yarn
  - yarn

script:
  - yarn lint
  - yarn test
  - yarn test:backbone1.2
  - SAUCELABS=true yarn test
  - yarn benchmark

after_success:
  - bash <(curl -s https://codecov.io/bash)
  - rm -rf coverage
